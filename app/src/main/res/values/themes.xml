<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Skyesque" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Skyesque" parent="Base.Theme.Skyesque">
        <item name="statusBarBackground">@color/dark_blue_card_background</item>
    </style>

    <style name="BrandedSwitch.control" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/light_blue_gradient</item>
        <item name="colorSwitchThumbNormal">@color/light_blue_card_background</item>
    </style>
</resources>